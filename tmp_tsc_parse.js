const fs = require('fs'); const ts = require('typescript'); const src = fs.readFileSync('app/realtime/page.tsx','utf8'); const sf = ts.createSourceFile('page.tsx', src, ts.ScriptTarget.Latest, true, ts.ScriptKind.TSX); const diag = sf.parseDiagnostics; console.log('diag count', diag.length); diag.forEach(d=>{ const pos = sf.getLineAndCharacterOfPosition(d.start); console.log(`${pos.line+1}:${pos.character+1} ${d.messageText}`); });